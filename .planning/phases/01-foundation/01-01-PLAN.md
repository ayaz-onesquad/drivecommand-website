---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/package.json
  - apps/marketing/tsconfig.json
  - apps/marketing/next.config.ts
  - apps/marketing/tailwind.config.ts
  - apps/marketing/postcss.config.mjs
  - apps/marketing/src/app/layout.tsx
  - apps/marketing/src/app/page.tsx
  - apps/marketing/src/app/globals.css
  - apps/marketing/src/lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "Running `npm run dev` from monorepo root serves the marketing app at a localhost port"
    - "All pages render Space Grotesk headlines and IBM Plex Sans body — no Inter, Roboto, or Arial anywhere"
    - "Brand color tokens (brand-dark, brand-blue, brand-amber, brand-green) are available as Tailwind utility classes"
    - "TypeScript strict mode is active and `npm run build` passes with zero type errors"
    - "The cn() utility is importable from '@/lib/utils'"
    - "The home page (/) has page-level SEO metadata exported from page.tsx"
  artifacts:
    - path: "apps/marketing/package.json"
      provides: "Workspace declaration with exact dep versions"
      contains: "@drivecommand/marketing"
    - path: "apps/marketing/src/app/layout.tsx"
      provides: "Root layout with fonts applied to <html>"
      contains: "spaceGrotesk.variable"
    - path: "apps/marketing/tailwind.config.ts"
      provides: "Brand colors + font family extensions"
      contains: "brand-dark"
    - path: "apps/marketing/src/app/globals.css"
      provides: "Tailwind directives + dark background default"
      contains: "@tailwind base"
    - path: "apps/marketing/src/lib/utils.ts"
      provides: "cn() helper"
      contains: "twMerge"
    - path: "apps/marketing/src/app/page.tsx"
      provides: "Home page with SEO metadata"
      contains: "export const metadata"
  key_links:
    - from: "apps/marketing/src/app/layout.tsx"
      to: "apps/marketing/tailwind.config.ts"
      via: "CSS variable font classes applied to <html>"
      pattern: "spaceGrotesk\\.variable.*ibmPlexSans\\.variable"
    - from: "apps/marketing/tailwind.config.ts"
      to: "apps/marketing/src/app/globals.css"
      via: "Tailwind content glob includes src/**"
      pattern: "content.*src/\\*\\*/\\*"
---

<objective>
Scaffold `apps/marketing/` as a new Next.js 15 App Router workspace inside the DriveCommand Turborepo. Configure the dual-font system (Space Grotesk + IBM Plex Sans), brand color tokens, Tailwind CSS setup, TypeScript strict mode, and the cn() utility.

Purpose: Every downstream plan depends on this shell existing with correct typography and color config. Plans 01-02 and 01-03 cannot start until this foundation is in place.
Output: A buildable Next.js app at apps/marketing/ with correct fonts, colors, Tailwind, and TypeScript — no navbar, footer, or pricing yet.
</objective>

<execution_context>
@/Users/ayazmohammed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayazmohammed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ayazmohammed/.planning/PROJECT.md
@/Users/ayazmohammed/.planning/STATE.md
@/Users/ayazmohammed/DriveCommand/package.json
@/Users/ayazmohammed/DriveCommand/turbo.json
@/Users/ayazmohammed/DriveCommand/apps/web/package.json
@/Users/ayazmohammed/DriveCommand/apps/web/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/marketing/ Next.js workspace</name>
  <files>
    apps/marketing/package.json
    apps/marketing/tsconfig.json
    apps/marketing/next.config.ts
    apps/marketing/postcss.config.mjs
  </files>
  <action>
    Create the `apps/marketing/` directory tree manually (do NOT run `create-next-app` interactively — write all files directly).

    **apps/marketing/package.json:**
    ```json
    {
      "name": "@drivecommand/marketing",
      "version": "0.1.0",
      "private": true,
      "scripts": {
        "dev": "next dev --port 3001",
        "build": "next build",
        "start": "next start --port 3001",
        "lint": "next lint"
      },
      "dependencies": {
        "next": "^16.2.1",
        "react": "^19.2.4",
        "react-dom": "^19.2.4",
        "motion": "^12.0.0",
        "lucide-react": "^0.564.0",
        "clsx": "^2.1.1",
        "tailwind-merge": "^3.4.1",
        "class-variance-authority": "^0.7.1",
        "@radix-ui/react-dialog": "^1.1.0",
        "@radix-ui/react-slot": "^1.2.0"
      },
      "devDependencies": {
        "@types/node": "^20",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "autoprefixer": "^10.4.0",
        "eslint": "^9",
        "eslint-config-next": "^16.2.1",
        "postcss": "^8",
        "tailwindcss": "^3.4.1",
        "typescript": "^5"
      }
    }
    ```

    **apps/marketing/tsconfig.json** — strict mode with path aliases:
    ```json
    {
      "compilerOptions": {
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": {
          "@/*": ["./src/*"]
        }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    **apps/marketing/next.config.ts:**
    ```typescript
    import type { NextConfig } from 'next'

    const nextConfig: NextConfig = {}

    export default nextConfig
    ```

    **apps/marketing/postcss.config.mjs:**
    ```javascript
    const config = {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }

    export default config
    ```

    After writing all files, run from the monorepo root:
    ```bash
    cd /Users/ayazmohammed/DriveCommand && npm install
    ```
    This installs `@drivecommand/marketing` as a workspace package and resolves all deps.
  </action>
  <verify>
    From `/Users/ayazmohammed/DriveCommand`:
    - `ls apps/marketing/` shows package.json, tsconfig.json, next.config.ts, postcss.config.mjs
    - `cat apps/marketing/package.json | grep '"name"'` outputs `"@drivecommand/marketing"`
    - `npm install` exits 0 (no errors)
  </verify>
  <done>apps/marketing/ is a valid npm workspace package with all config files present and dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind, fonts, globals, layout, and utils</name>
  <files>
    apps/marketing/tailwind.config.ts
    apps/marketing/src/app/globals.css
    apps/marketing/src/app/layout.tsx
    apps/marketing/src/app/page.tsx
    apps/marketing/src/lib/utils.ts
  </files>
  <action>
    Create all source files for the marketing app foundation.

    **apps/marketing/tailwind.config.ts** — brand colors + font families (raw hex, NOT HSL CSS vars — marketing site is dark-only, no theming needed):
    ```typescript
    import type { Config } from 'tailwindcss'

    const config: Config = {
      content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
      theme: {
        extend: {
          colors: {
            brand: {
              dark:  '#0f172a',
              blue:  '#3b82f6',
              amber: '#f59e0b',
              green: '#10b981',
            },
          },
          fontFamily: {
            display: ['var(--font-display)', 'sans-serif'],
            body:    ['var(--font-body)',    'sans-serif'],
          },
        },
      },
      plugins: [],
    }

    export default config
    ```

    **apps/marketing/src/app/globals.css:**
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      html {
        scroll-behavior: smooth;
      }
      body {
        @apply bg-brand-dark text-slate-100 font-body antialiased;
      }
      h1, h2, h3, h4, h5, h6 {
        @apply font-display;
      }
    }
    ```

    **apps/marketing/src/app/layout.tsx** — dual font setup with CSS variables (CRITICAL: both must have `variable` option; both `.variable` classes must be on `<html>`):
    ```typescript
    import type { Metadata } from 'next'
    import { Space_Grotesk, IBM_Plex_Sans } from 'next/font/google'
    import './globals.css'

    const spaceGrotesk = Space_Grotesk({
      subsets: ['latin'],
      variable: '--font-display',
      display: 'swap',
    })

    const ibmPlexSans = IBM_Plex_Sans({
      subsets: ['latin'],
      weight: ['400', '500', '600', '700'],
      variable: '--font-body',
      display: 'swap',
    })

    export const metadata: Metadata = {
      metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'https://drivecommand.com'),
      title: {
        default: 'DriveCommand — Fleet Management Platform',
        template: '%s | DriveCommand',
      },
      description: 'Fleet management built for modern carriers. Dispatch, track, invoice, and stay compliant — all in one platform.',
      openGraph: {
        siteName: 'DriveCommand',
        type: 'website',
        locale: 'en_US',
      },
      twitter: {
        card: 'summary_large_image',
      },
      robots: {
        index: true,
        follow: true,
      },
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html
          lang="en"
          className={`${spaceGrotesk.variable} ${ibmPlexSans.variable}`}
        >
          <body>{children}</body>
        </html>
      )
    }
    ```

    DO NOT add `metadata.viewport` or `metadata.themeColor` — these are deprecated since Next.js 14 and silently ignored. If viewport/themeColor are ever needed, add a separate `export const viewport: Viewport` export.

    **apps/marketing/src/app/page.tsx** — minimal placeholder with SEO metadata. Plan 01-02 will wire in Navbar/Footer via the root layout; this page provides its own page-level title override and description so the home route has correct `<title>` and Open Graph tags:
    ```typescript
    import type { Metadata } from 'next'

    export const metadata: Metadata = {
      title: 'Fleet Management Platform for Modern Carriers',
      description: 'DriveCommand gives independent operators and growing fleets one place to dispatch, track, invoice, and stay compliant.',
      openGraph: {
        title: 'DriveCommand — Fleet Management Platform',
        description: 'Dispatch, track, invoice, and stay compliant — all in one platform.',
        type: 'website',
      },
    }

    export default function HomePage() {
      return (
        <main className="min-h-screen flex items-center justify-center">
          <h1 className="font-display text-4xl text-white">DriveCommand</h1>
        </main>
      )
    }
    ```

    **apps/marketing/src/lib/utils.ts:**
    ```typescript
    import { clsx, type ClassValue } from 'clsx'
    import { twMerge } from 'tailwind-merge'

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    After writing files, run from monorepo root:
    ```bash
    cd /Users/ayazmohammed/DriveCommand && npx turbo run build --filter=@drivecommand/marketing
    ```
    This verifies the app compiles. If turbo isn't available, run:
    ```bash
    cd /Users/ayazmohammed/DriveCommand/apps/marketing && npx next build
    ```
  </action>
  <verify>
    - `npx turbo run build --filter=@drivecommand/marketing` exits 0 from `/Users/ayazmohammed/DriveCommand`
    - Build output shows no TypeScript errors
    - `grep -r "Inter\|Roboto\|Arial" apps/marketing/src/` returns no matches
    - `grep "var(--font-display)" apps/marketing/tailwind.config.ts` returns a match
    - `grep "spaceGrotesk.variable.*ibmPlexSans.variable" apps/marketing/src/app/layout.tsx` returns a match
    - `grep "export const metadata" apps/marketing/src/app/page.tsx` returns a match
  </verify>
  <done>
    Marketing app builds without errors. Space Grotesk and IBM Plex Sans are configured via CSS variables. Brand color tokens are available. The cn() utility is importable from '@/lib/utils'. No Inter, Roboto, or Arial present anywhere. Home page exports page-level SEO metadata.
  </done>
</task>

</tasks>

<verification>
From `/Users/ayazmohammed/DriveCommand`:
1. `ls apps/` shows `marketing/` alongside `web/` and `mobile/`
2. `npx turbo run build --filter=@drivecommand/marketing` exits 0
3. `grep -r "Inter\|Roboto\|Arial" apps/marketing/` returns no results
4. `cat apps/marketing/src/app/layout.tsx | grep "spaceGrotesk.variable"` returns a match
5. `cat apps/marketing/tailwind.config.ts | grep "brand-dark\|#0f172a"` returns a match
6. `grep "export const metadata" apps/marketing/src/app/page.tsx` returns a match
</verification>

<success_criteria>
- apps/marketing/ is a registered npm workspace with `"name": "@drivecommand/marketing"`
- `npx turbo run build --filter=@drivecommand/marketing` exits 0 with no type errors
- Space Grotesk (`--font-display`) and IBM Plex Sans (`--font-body`) are configured in layout.tsx with both `.variable` classes applied to `<html>`
- Tailwind config has brand colors (dark/blue/amber/green) and fontFamily extensions (display/body)
- `@/lib/utils` exports cn()
- Zero forbidden fonts (Inter, Roboto, Arial) in the entire apps/marketing/ tree
- apps/marketing/src/app/page.tsx exports `metadata` with page-level title and description
</success_criteria>

<output>
After completion, create `/Users/ayazmohammed/.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
